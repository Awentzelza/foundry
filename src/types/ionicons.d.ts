/**
 * Type stub for `ionicons/icons` — the package ships its icon constants as a
 * single .js file without an accompanying .d.ts. We treat each named import
 * as an opaque string. The wildcard signature catches every icon name.
 */
declare module 'ionicons/icons' {
  export const add: string;
  export const addCircle: string;
  export const checkmarkCircle: string;
  export const close: string;
  export const closeCircle: string;
  export const ellipseOutline: string;
  export const trashOutline: string;
  export const trash: string;
  export const settings: string;
  export const settingsOutline: string;
  export const home: string;
  export const star: string;
  export const starOutline: string;
  export const pause: string;
  export const play: string;
  export const refresh: string;
  export const refreshOutline: string;
  export const stopCircle: string;
  // Catch-all for any other named icon import.
  const _all: { readonly [key: string]: string };
  export = _all;
}
